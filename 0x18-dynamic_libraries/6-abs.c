#include "main.h"
/**
*abs-computes  the absolute value of an integer.
*@a:the integer to compute its absolute value.
*iReturn:returns the abs value 
 */ 
int _abs(int t)

{
	if (t >= 0)
		
		return (t);
	else 
		return(-t);
}
